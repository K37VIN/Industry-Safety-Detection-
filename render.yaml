services:
  - type: web
    name: isd-yolov7-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
    region: oregon
    plan: free
